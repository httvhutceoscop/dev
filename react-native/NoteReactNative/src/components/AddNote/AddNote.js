import React, {Component} from 'react';
import {StyleSheet, Text, View} from 'react-native';
import styles from './styles';

export default class AddNote extends Component {
  render() {
    return (
      <View style={styles.container}>
        <Text>Add Notes 222</Text>
      </View>
    );
  }   
}